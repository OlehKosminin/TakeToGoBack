export default interface IProduct {
  id: number;
  title: string;
  description: string;
  price: string;
  currency: string;
  category: string;
  bought: number;
  photoUrl: string;
  publicId: string;
  alt: string;
  discount: string;
  weight: string;
  ingredients: string;
}
